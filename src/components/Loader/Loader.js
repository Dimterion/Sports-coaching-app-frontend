import "./loader.css";

/**
 * Component for the loading message.
 * @example
 * returns (
 *  <Loader />
 * )
 */

function Loader() {
  return <h2 className="loader-container">Page is loading...</h2>;
}

export default Loader;
